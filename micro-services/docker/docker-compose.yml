# This file delegates to the repository-root Compose stack so `.env` resolves in one place.
#
# From repo root (recommended):
#   docker compose up -d
#
# From this directory:
#   docker compose -f ../../docker-compose.yml up -d

include:
  - path: ../../docker-compose.yml
