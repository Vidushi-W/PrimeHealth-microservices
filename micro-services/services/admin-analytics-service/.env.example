# Service-level env template (for running admin service outside Docker compose)
PORT=5001
MONGO_URI=mongodb://localhost:27017/admin_analytics
JWT_SECRET=replace-with-strong-secret

# Optional: only needed when running npm run seed:admin
ADMIN_SEED_EMAIL=
ADMIN_SEED_PASSWORD=
