{
  "name": "primehealth-frontend",
  "private": true,
  "scripts": {
    "dev": "npm --prefix admin-analytics-client run dev",
    "build": "npm --prefix admin-analytics-client run build",
    "preview": "npm --prefix admin-analytics-client run preview",
    "lint": "npm --prefix admin-analytics-client run lint"
  }
}
